# @defaultRequired=true
# @defaultSensitive=true
# @currentEnv=$NODE_ENV
# @generateTsTypes(path=./src/env.ts, exposeEnv=local)
# ---

# @public @type=enum(development, production, test)
NODE_ENV=development

# @type=string(minLength=32)
BETTER_AUTH_SECRET=

# @public @dynamic @type=url
BETTER_AUTH_URL=

# @type=string(minLength=1)
DATABASE_URL=

# === OpenSearch: hybrid vector + keyword document index ===

# @type=string(minLength=1) @sensitive=false
OPENSEARCH_HOST=localhost

# @type=number @sensitive=false
OPENSEARCH_REST_API_PORT=9200

# @type=string(minLength=1) @sensitive=false
OPENSEARCH_ADMIN_USERNAME=admin

# @type=string(minLength=1)
OPENSEARCH_ADMIN_PASSWORD=

# @type=boolean @sensitive=false
OPENSEARCH_USE_SSL=true

# Self-signed cert by default; set true only with a real CA bundle.
# @type=boolean @sensitive=false
OPENSEARCH_VERIFY_CERTS=false

# === Redis: background indexing queue ===

# @type=string(minLength=1)
REDIS_URL=redis://localhost:6379

# === Object storage for uploaded originals (MinIO by default) ===

# @type=string(minLength=1) @sensitive=false
S3_ENDPOINT=http://localhost:9000

# @type=string(minLength=1)
S3_ACCESS_KEY_ID=

# @type=string(minLength=1)
S3_SECRET_ACCESS_KEY=

# @type=string(minLength=1) @sensitive=false
S3_BUCKET=onirix-file-store

# === Model provider credentials ===
# Optional server-side keys. When set, the onboarding flow offers the provider
# without asking the user to paste a key, and the key is never copied into the
# database. A workspace may still supply its own key instead.

# @required=false
OPENAI_API_KEY=

# @required=false
GEMINI_API_KEY=

# @required=false
XAI_API_KEY=

# === Google OAuth (optional social sign-in) ===
# Declared so varlock does not flag them; wiring social sign-in is a separate step.

# @required=false @sensitive=false
GOOGLE_CLIENT_ID=

# @required=false
GOOGLE_CLIENT_SECRET=
